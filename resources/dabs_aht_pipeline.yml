# The main pipeline for dabs_aht
resources:
  pipelines:
    dabs_aht_pipeline:
      name: dabs_aht_pipeline
      target: dabs_aht_${bundle.environment}
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb
